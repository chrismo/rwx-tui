# GoReleaser config for crux. Cuts a GitHub Release with cross-compiled
# archives + checksums and updates the Homebrew tap formula on each `vX.Y.Z` tag.
#
# Validate:  goreleaser check
# Dry run:   goreleaser release --snapshot --clean   (no publish)
version: 2

project_name: crux

before:
  hooks:
    - go mod tidy

builds:
  - id: crux
    main: ./cmd/crux
    binary: crux
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }} -X main.commit={{ .ShortCommit }} -X main.date={{ .Date }}
    goos: [darwin, linux]
    goarch: [amd64, arm64]

archives:
  - id: crux
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

release:
  # The GitHub Release is published on the source repo.
  github:
    owner: chrismo
    name: crux

homebrew_casks:
  - name: crux
    repository:
      owner: chrismo
      name: homebrew-crux
      # No explicit token: released locally, so GoReleaser uses the default
      # GITHUB_TOKEN — set to your gh token (repo scope), which can push to the
      # tap. (A CI release would need a cross-repo PAT here instead.)
    homepage: "https://github.com/chrismo/crux"
    description: "Local TUI for monitoring RWX runs with a better Flow graph"
    # Strip the Gatekeeper quarantine xattr so the unsigned binary runs.
    hooks:
      post:
        install: |
          if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/crux"]
          end
